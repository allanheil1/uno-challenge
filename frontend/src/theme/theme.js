import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#66ccaa",
    },
    secondary: {
      main: "#66d7f2",
    },
    tertiary: {
      main: "#66bbe4",
    },
    white: {
      main: "#ffffff",
    },
    background: {
      default: "#fcfcfc",
    },
  },
});

export default theme;
